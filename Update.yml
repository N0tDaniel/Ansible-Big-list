---
- name: Update and Upgrade
  hosts: your_target_host
  become: yes
  tasks:
    - name: Update APT package cache
      apt:
        update_cache: yes
      become: yes

    - name: Upgrade all packages
      apt:
        upgrade: dist
      become: yes
